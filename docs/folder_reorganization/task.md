# タスク: フォルダ構成の整理と冗長性の排除

- [ ] ディレクトリ作成 (`hooks`, `types`, `components`, `utils`)
- [ ] 型定義の集約 (`types/index.ts` の作成と移行)
- [ ] ユーティリティの分割 (`utils/image.ts`, `utils/date.ts`)
- [ ] コンポーネントの移動 (`components/shared`, `components/room`)
- [ ] カスタムフックの移動 (`hooks/useRoomData.ts`)
- [ ] 全ファイルのインポートパス修正
- [ ] 共通認証フックの実装 (`hooks/useAuth.ts`)
- [ ] 各ページへの `useAuth` 適用と重複コード削除
- [ ] 共通 UI コンポーネントの実装 (`components/shared/BrandHeader.tsx`)
- [ ] 動作確認とビルドチェック
